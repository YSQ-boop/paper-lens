interface:
  display_name: "Paper Lens"
  short_description: "Quick reads and reviewer-level reports for one paper"
  default_prompt: "Use $paper-lens to quickly read this paper, then deepen the same report when requested."
